# Approved operations activation

The user approved Better Stack, Backblaze B2, 30-day recovery history and the USD 10/month starting allowance on 2026-09-18, and supplied the alert email. Approval is recorded separately from activation; the private email/status record is under ignored `.sites-runtime/operations/activation.json`.

## Current state

- Public production health passed again on 2026-09-18.
- Better Stack onboarding completed on 2026-09-18 using the Free plan. Optional phone-call testing and colleague invitations were skipped; the selected dark theme was preserved.
- Three email-only monitors are configured at three-minute intervals: homepage HTTP 200 (4947538), API health HTTP 200 (4947595), and API health content (4947606). Confirmation and recovery periods are three minutes; request timeout is fifteen seconds; SSL verification is on and redirects are off.
- Better Stack confirmed a test alert was sent. The user explicitly confirmed receipt on 2026-09-18. The free plan notifies the entire team; no colleagues were invited during setup.
- Backblaze bucket `relay-backups-20260918-a72c` was created and verified private, with default SSE-B2 encryption, all versions retained, and Object Lock disabled. Its endpoint is `s3.us-east-005.backblazeb2.com`. The account's existing free usage caps remain in place; the USD 10 allowance is not a configured spending cap.
- The browser's Write Only preset unexpectedly granted `deleteFiles` and bucket-setting permissions. The unused `relay-backup-writer` key was revoked with explicit user approval, and its removal was verified in the key list. It was never used for backups.
- Both custom keys were created on 2026-09-18 using the requests in `deploy/backblaze-key-requests.json`. A fresh authorization independently verified the writer has only `writeFiles`, and the restore reader has only `listFiles` and `readFiles`; both are restricted to this bucket and the `relay/` prefix. Neither has deletion or bucket-setting capabilities.
- The user entered the saved master key into a loopback-only setup form. The helper used it without saving it, encrypted each returned restricted key with Windows DPAPI under ignored `.sites-runtime/operations`, and closed after success. Separate non-secret verification records retain the actual scopes and verification timestamps. These encrypted files require this Windows user's DPAPI access and are not a portable disaster-recovery credential escrow.
- The manual copy job and isolated restore checker are implemented in `scripts/relay-backup.mjs`. The first recovery point, `2026-09-18T05-28-23-960Z-2c915f67-5b02-4bac-8d95-71562b5cff0a`, was copied to Backblaze and restored successfully on 2026-09-18. All five completed originals (233,197,388 bytes) matched SHA-256 and size; the restored database passed integrity and foreign-key checks. It contained two spaces, seven devices, nine invitations and five media records. All old sessions and invitations were invalidated in the isolated restore only.
- The first isolated restore took 24 seconds; this is measured local validation time, not a production recovery-time commitment. Current production health passed after the backup and restore.
- A second snapshot, `2026-09-18T05-30-32-688Z-1ec298b4-2a7b-4adf-87dd-2edf74542bfc`, reused all five original versions (zero duplicate original uploads) and passed a separate full restore check in 23 seconds. Backblaze's catalog confirmed five originals plus four snapshot files, totaling 233,227,942 bytes, all encrypted with SSE-B2. Targeted lint and five recovery tests passed, including hash mismatch, broken relationships, access revocation, Trash inclusion and credential-scope rejection.
- Better Stack heartbeat `494846` was created for daily verified backups with a one-day period, two-hour grace and email alerts. It was activated by the first successful GitHub restore on 2026-09-18 at approximately 06:16 UTC. Better Stack was read back as Up with a fresh recorded heartbeat and zero heartbeat incidents. `scripts/backup-heartbeat.mjs` rejects stale, incomplete, mismatched or failed restore reports before any success request; all nine backup/heartbeat tests and targeted lint passed.
- The user selected GitHub-hosted execution. A four-job daily workflow is prepared locally, separating read-only inventory, upload, restore verification and failure reporting. Three main-only GitHub environments are configured. The B2 writer is stored only in the copy environment, the reader only in the verification environment, and the shared inventory-encryption key only in those two environments. The heartbeat URL is saved in the verification and alert environments. The user approved both source-read credentials. They were created and stored as encrypted GitHub environment secrets. The bucket-restricted R2 reader passed an original-file download with matching size and SHA-256. The D1:Read token is active and can read Relay database metadata (HTTP 200), but both Wrangler export and a direct export request failed; the direct request returned HTTP 401 / code 10000. Read-only export is therefore not usable with this credential. No permissions were broadened. A subsequent investigation verified a SELECT-only logical snapshot with the existing token: all four application tables restored with integrity and foreign-key checks passing, and Cloudflare reported zero rows written. The approved production implementation now passes 19 targeted tests and lint. Snapshot 2026-09-18T06-11-41-754Z-34aa8daf-ade7-47f0-ba2e-df5b2de53489 was created using the environment-secret path, reused all five originals, and passed a full independent Backblaze restore in 24 seconds (233,197,388 original bytes). Database integrity, foreign keys and restored access revocation passed. See READ-ONLY-BACKUP-INVESTIGATION.md. Broader D1 access is unnecessary for this approach. The user approved publication and activation. Commit 6fed28e was pushed to main, and [GitHub run 35314135923](https://github.com/kiwanukaphil-oss/media-sharing-app/actions/runs/35314135923) passed inventory, copy and independent restore verification. Snapshot 2026-09-18T06-16-34-688Z-bdd985f9-31f8-4880-96b4-35094800f6e7 protected all five originals (233,197,388 bytes), reused the existing original versions, and reported success to Better Stack. The workflow is active for 00:00 UTC / 03:00 Nairobi daily. The failure-report job was correctly skipped on success. Main-only environment restrictions were rechecked, and the push's Web checks passed.
- The bucket retains all versions. Automatic 30-day pruning remains inactive; all versions are retained. The daily runner and backup heartbeat are active following a successful manual dispatch on GitHub. The first cron-triggered run is still future; GitHub scheduling remains best effort. See `BACKUP-RECOVERY.md` for commands, credential separation, retention limitations and cloud-cutover requirements.
- Account preparation did not deploy the application or migrate its schema. The separately approved operations publication committed and pushed only the prepared backup/monitoring files; unrelated application changes remain local.

## Prepared monitoring configuration

`deploy/monitoring.json` contains the proposed create-request bodies for three public checks: homepage status, API status and API response content. Splitting API status and content explicitly checks both requirements. Checks run every 180 seconds, with 180-second confirmation/recovery periods and email-only notifications. The confirmation period means alerting is not guaranteed within three minutes of an outage.

The backup heartbeat expects a verified successful run every 86,400 seconds with a 7,200-second grace period. Create/activate it only when the real backup job is ready; never send successful heartbeats for an unconfigured or incomplete backup. The daily job target remains 03:00 Africa/Nairobi (00:00 UTC).

The request format was checked against [Better Stack monitor documentation](https://betterstack.com/docs/uptime/api/create-a-new-monitor/) and [heartbeat documentation](https://betterstack.com/docs/uptime/api/create-a-hearbeat/). The Free plan accepted the monitor setup, and test notification delivery was confirmed by the user. Advanced on-call schedules require an upgrade; the free plan instead notifies the team. The JSON does not contain API tokens, recipient email or a heartbeat credential.

## Continue after sign-in

1. Verify the signed-in account and alert recipient. Confirm the free plan permits the chosen monitoring settings; do not add paid extras beyond the approved allowance.
2. Create the public checks in the intended team and verify their returned settings and live healthy state. Trigger a clearly named test notification to the approved email without taking the app offline; verify receipt before declaring alerts active.
3. Verify the Backblaze account region and create a private, encrypted backup bucket. Keep billing controls within the approved allowance; a budget warning is not a hard spending cap.
4. Prepare separate least-privilege source-read and backup-write credentials, retaining recovery credentials outside the app. Do not send credentials through chat or commit them.
5. Implement and verify incremental media copies, consistent database exports/manifests and 30-day recovery retention as described in `OPERATIONS-PROPOSAL.md`. The routine copy identity must not be able to purge history. Do not apply a blanket 30-day upload-age deletion rule to unchanged originals.
6. Complete a first backup and restore into isolated storage, including original-file SHA-256 checks and revoked-session handling. Only then enable the heartbeat and daily runner.
7. Read back the active configuration and update this status with evidence. Any repository commit/push remains subject to the user's separate confirmation rule.

## Identity and recovery monitoring - 21 September 2026

The existing Free team now has four HTTP monitors and its original daily backup heartbeat. New monitor **4956708**, Relay - identity and recovery checks, reads `https://relayalbums.com/api/operations/health` without credentials. It requires HTTP 200, checks every 180 seconds, confirms/recoveries after 180 seconds, verifies TLS, refuses redirects and sends email only. Saved configuration and Up state verified. Better Stack confirmed the explicitly authorised test alert was sent; receipt is pending. The generic Billable label did not mean a paid upgrade was required; none was selected.

Worker 75e9e54e-c94b-47e4-91d7-3d3d0608855e stores a minimal signed combined status. Provider/D1 checks pass in hosted runs 35580179355 and 35580503181. Live signed failure and subsequent real-check recovery were verified before creating the external monitor, without interrupting app access. Missing/corrupt/failed or over-90-minute-old status returns 503; local real-R2 tests verify expiry and concurrency. This remains detection, not immediate revocation or a guaranteed alert deadline. Independent backup monitoring is unchanged.
